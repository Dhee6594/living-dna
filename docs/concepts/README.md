# Concepts

The mental model behind Living DNA. Placeholder pages to be filled in:

- **The genome** — the bitemporal mini-graph (nodes, edges, events, profiles).
- **DNA profiles** — the materialized per-service read cache.
- **Eras** — activity-segmented time windows; the unit the miner reasons over.
- **Bitemporality** — world time (`valid_from`/`valid_to`) vs. knowledge time (`recorded_at`).
- **Archaeology** — graph-first Q&A with evidence.

Canonical source today: [`../../ARCHITECTURE.md`](../../ARCHITECTURE.md) §5.
