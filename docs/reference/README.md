# Reference

Precise, generated-where-possible reference material. Placeholder — to be filled in:

- **CLI** — every `dna` subcommand and flag (`ingest profile ask busfactor timetravel
  diff mine export serve report insights`).
- **JSON API** — `/api/profiles`, `/api/profile/<svc>`, `/api/graph`, `/api/diff`,
  `/api/busfactor`, `/api/ask`, `/api/people`, `/api/decisions`, `/api/insights`,
  `/api/report`, `/api/search`, `/api/events`.
- **Ontology** — node kinds, edge kinds, event kinds, and the export schema version.
- **Configuration** — environment variables and defaults.
